/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "maps.googleapis.com",
      "site--petitegraine--xj5ljztnmr2k.code.run/",
    ],
  },
};

export default nextConfig;
